interface IGiftIdeasCardProps {
    title: string,
    // handler: () => void,
    data: any,
    isLoading: boolean,
    error: any,
}

  const GiftIdeasCard = (props: IGiftIdeasCardProps) => {

    // const { title, data, isLoading } = props;


    return(
        <>

        
        </>
    );
  }

  export default GiftIdeasCard;